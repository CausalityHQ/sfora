# Outcome-only Fable continuation: no eligible candidate

Date: 2026-08-04.

## Brief

Fable received the similarity-learning problem, the audited comparable
horizons (0.766 CUB, 0.949 Cars196, 0.939 In-Shop), the data/capacity limits,
and the scientific gates. It was not given a proposed mechanism and was told
to return at most one method or `NONE`.

## First pass: invalid isolation, useful collision

The completed pass returned **NONE**. Its single proposed missing measurement
was test-time Sinkhorn-OT correspondence headroom on the corrected In-Shop
Proxy Anchor checkpoint. Contingent on that measurement, it described a
label-supervised spatial-OT teacher distilled into the deployed global cosine
descriptor.

This does not reopen the search. It is exact candidate 368 / candidate 146:
DIML supplies supervised structural matching, S2SD supplies
similarity-to-deployed-descriptor distillation, and global-local
self-distillation is an additional direct neighbour. Repository evidence also
points against the premise: trained regional supervision lost about 3.6 CUB
points and a frozen Cars MaxSim readout lost 1.47 points. Running another OT
headroom sweep would repeat a dead action map.

The pass is not an independent convergence result. Its web tools were denied,
its internal searches fell back to repository files, and it read the candidate
ledger despite the outcome-only instruction. The collision is valid because
the executable proposal can be compared to the ledger; the claimed independent
invention is not.

## Corrected retry

A fresh Fable session was started with local Read, Glob, Grep, Bash, Write,
Edit, and NotebookEdit disabled, with only WebSearch and WebFetch allowed. It
received the outcome-only brief again. The account reached its Fable session
limit before the model returned; the limit reported a reset at 23:50 UTC.

After reset, the exact prompt was rerun. The first post-reset execution was
invalidated when the command-line client's default 600-second background-worker
ceiling killed four still-running literature searches. The unchanged prompt was
then run with the background ceiling disabled. That corrected web-only,
filesystem-denied execution completed and returned **`NONE`**. Its fallback
error-decomposition measurement was rejected after audit because its buckets
overlap, its metric-oracle branch is label-leaky or non-identifying depending on
the split, its action branches collide with existing audits, and its stated cost
omits the 12 folded training runs per dataset. Full record:
`docs/fable_repository_blind_outcome_result_2026-08-05.md`.

## Current ruling

There is **no new eligible candidate from this continuation** and no reason to
spend GPU on either the repeated OT-distillation measurement or the later
omnibus error decomposition. Candidate 370's separate primary-source re-audit
is recorded in `docs/recomb_hse_primary_reaudit_2026-08-04.md`; HSE strengthens
its existing dead verdict. The GPU remains idle because no proposal currently
clears both provenance and prior-art gates.

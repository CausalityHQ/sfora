# Pass 60 blind proposal — NONE

The proposer returned NONE. Its reusable result is the **orientation-free gain
barrier**: a mechanism invariant to descriptor rotations, or selective only by
second-order statistics or seen-label partitions, can change local
within-class error and between-class overlap only through a scalar contraction
trade-off. The measured CUB split (48.1% local, 51.9% between) is near that
trade-off’s stationary point.

The proposer closed bi-Lipschitz heads, whitening/isotropy, MCR², kernel
pooling, attractor/DEQ heads, IRM/V-REx with exchangeable classes,
topological/MST losses, and initialization-anchored distillation. An exactly
equivariant backbone with invariant readout is the only principled direction-
selective survivor, but no audited ImageNet-initialized Lane-A backbone exists
for it. No forecast or GPU run followed.

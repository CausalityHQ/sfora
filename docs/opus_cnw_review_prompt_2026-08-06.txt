Provider: fresh Fable session with configured Claude Opus fallback, independent
cold adversarial reviewer
Frozen before review consultation start: 2026-08-06 UTC

Read exactly:

  /home/rb/worktrees/sfora-emafactorial/docs/opus_cnw_proposal_pass40_2026-08-06.md

Do not edit repository files or inspect git history, local audits, prior reviews,
the verdict ledger, or conversations. Start cold from the frozen proposal and
primary sources. Return LIVE, DEAD, or UNRESOLVED with the earliest failed gate
and one decisive mechanism-level reason. This is adjudication, not repair. Do
not silently improve, reinterpret, or replace the frozen object.

Problem and legal envelope: train on the official training split with identity
labels, ordinary image augmentation, and ImageNet-1K initialization only. Test
identities are disjoint. No test images, gallery statistics, external generated
data, text model, reranking, transduction, ensemble, or test-time fitting may
enter training or selection. Deployment is one single-view image encoder, one
512-D L2-normalized descriptor, and ordinary cosine nearest-neighbour retrieval.
All train-only machinery must disappear at deployment. Hyperparameters may be
selected only through a class-disjoint split of training identities.

Audited matched Lane-A references are ResNet-50/512-D/single-view PFML over five
runs: CUB 0.734 +/- 0.003, Cars196 0.927 +/- 0.003, SOP 0.829 +/- 0.002. These
published results are credible horizons but their complete authors' executable
recipe is unavailable locally. Higher-capacity single-view observations are
VAPNet ResNet-50/2048-D at CUB 0.762, Cars 0.948, In-Shop 0.939; AdvRF at CUB
0.766 and Cars 0.949; and CRT MiT-B2/128-D at In-Shop 0.9448. They have no
reported multi-seed uncertainty and belong to separate capacity lanes, but a
general SOTA claim must confront them. The corrected local In-Shop Proxy Anchor
seed 0 is raw 0.9163 and final 0.9137; one seed is not a fixed baseline. A live
candidate must first establish a same-seed corrected In-Shop paired control,
then confirm out of sample and on a second dataset. Report raw and independently
selected/final metrics; best-over-test-training is not valid selection.

Before recommending implementation or GPU work, independently:

1. Reproduce the exact loss, rank ceiling, participation-ratio range, gradient
   through smoothed residual normalization and class centering, sampler, gate,
   warm-up, and empty/small-K behavior. Decide whether each operation is
   uniquely executable.
2. Identify the exact zero set. Construct the cheapest configurations involving
   class-private orthogonal low-rank residual subspaces, a shared low-rank
   subspace, class-dependent scales, residual collapse, nuisance-only scatter,
   and non-orthogonal encoder reparameterizations. Decide whether pooled
   spectral flatness implies per-identity covariance isotropy or transfer to an
   unseen identity.
3. Distinguish the second moment of normalized residual directions from the
   covariance of unnormalized residuals. Test the claims of scale invariance,
   equal weighting, trace preservation, orthogonality to compactness, and the
   stated Jacobian when epsilon is nonzero.
4. Audit the Bayes, Jensen, minimax, and SNR arguments. Derive the convexity
   range of `Phi(-a/sqrt(v))`; test the uniform-direction, fixed-separation,
   common-covariance, fixed-trace, Gaussian, prototype, sphere, and
   nearest-neighbour assumptions. Decide whether isotropic within-class noise
   is actually the unique optimum for zero-shot retrieval.
5. Search primary prior art aggressively inside and outside DML: DVML;
   Non-Isotropy Regularization; class-conditional Gaussianity/shared-covariance
   losses; WCCN and PLDA; learned retrieval whitening; class-wise covariance
   regularization/decorrelation; whitening losses; VICReg/Barlow/W-MSE;
   discriminant and Mahalanobis learning; probabilistic embeddings; neural
   collapse; and supervised conditional-covariance/effective-rank objectives.
   Judge the supervision target and action, not the exact participation-ratio
   wrapper or whether whitening occurs inside the encoder instead of after it.
6. Resolve the proposal's two disclosed collision risks from primary sources:
   Deep Metric Learning with Density Adaptivity and Distance Metric Learning
   with Joint Representation Diversification. Do not infer their mechanisms
   from search snippets.
7. Audit causal provenance. Forecasted `O(10)` rank, overlap, R@1, literature
   diagnosis, or minimax algebra is not a repository measurement. State what
   prospective measurement would be required to connect CNW to corrected
   identity-disjoint official-query errors.
8. Audit controls C1--C10 for whether they isolate covariance shape from pooled
   private-subspace allocation, ordinary compactness, marginal whitening,
   sampler changes, and train-fitted linear WCCN. Check whether any control
   relies on test statistics or changes compute/exposure.
9. Check protocol compliance: corrected In-Shop must be screened first after
   Gates 1--3, trainable BatchNorm must match the corrected recipe, and every
   reported arm needs raw best plus independently selected/final metrics.
   Determine whether taking the stronger of two baseline schedules is legal as
   specified.
10. Recompute FLOPs, memory, wall-clock, all uncertainty and crossing arithmetic,
    and compare matched Lane-A claims separately from general SOTA claims.

Preserve correct subcomponents separately from the verdict. Cite primary
sources and state unresolved uncertainty. A substantive repair is a new
proposal and must not be used to upgrade this frozen one.

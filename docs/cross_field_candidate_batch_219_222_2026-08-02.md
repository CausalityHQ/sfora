# Cross-field candidate batch 219--222

Date: 2026-08-02. Generated and adversarially reduced with Claude before any
diagnostic, implementation, or GPU work.

## Provenance filter

Two recent measurements cannot seed a method under their own registered rules.
The OPIS/error rhos **0.135--0.180** were explicitly inconclusive and authorize
neither a method nor GPU. The faithful Cars RS@k result **0.793260 raw / 0.788987
corrected** is an occupied reference and selection-bias measurement, not a new
observable. This batch used only the ARCG relation, stable In-Shop partitions,
pre-normalisation magnitude, and proxy ownership as possible provenance.

## 219. Response-agreement negative admissibility

**DEAD at Gate 1 and Gate 2.** The proposal retained Proxy Anchor's positive
term but changed which different-class pairs remain negatives according to
controlled-transform response agreement. This would avoid ARCG's self-erasure,
but its claimed provenance is invalid: ARCG's **0.3631** density, closest-
quartile rejection **53.37%**, and farthest-quartile acceptance **28.00%** were
all measured only among same-class pairs. The repository has not measured
response agreement for different-class pairs.

Even a positive diagnostic could not establish novelty. Removing suspected
false negatives is false-negative cancellation or negative-pair mining;
candidate 104 already closes negative-to-unknown editing. Changing its estimator
from embedding similarity to response similarity does not change the discrete
supervision object.

## 220. Latent structured partition with loss-augmented inference

**DEAD at Gate 1 and Gate 2.** Cross-seed partition ARI **0.84148** is real, but
the partition's causal direction is unidentified and it aligns strongly with
filename acquisition series (ARI **0.754--0.761**). Treating the partition as a
latent structured output and optimizing a partition-level task loss remains
latent clustering/subcentres; loss-augmented retrieval inference remains
listwise direct-loss optimization. Structured-prediction terminology does not
create a new observed relation.

## 221. Set-valued embeddings with containment supervision

**DEAD at Gate 1, Gate 2, and deployment.** The proposed image ball used
controlled-response dispersion as radius and supervised same-class containment.
The repository's available quality/radius indicators are weak or destructive:
direct raw dot-product similarity loses **24.314 points**, raw Euclidean loses
**2.201 points**, and stochastic crop-tail scale failed repeatability after
within-class residualisation. HIB, probabilistic face embeddings, IDML,
chance-constrained DML, and order/box embeddings occupy set/distribution-valued
representations. A single 512-D cosine vector discards the radius at test.

## 222. Latent abstention on identity membership

**DEAD at Gate 2 and the independence requirement.** The large ownership/R@1
gap (**0.9656** versus **0.8865**) suggested declaring some training identity
relations undecidable. But ownership is computed from the model's current proxy
ranking and is therefore endogenous. Discrete absence is sample/interaction
selection or self-paced/co-teaching; soft absence is weighting; assignment
repair is candidates 184/213. The proposal supplies no independent observable
and no new supervision object.

## Verdict

**NONE survives.** Candidate 219 is the sharpest process lesson: provenance
must measure the same relation type the intervention edits. A same-class
response graph cannot justify a different-class eligibility rule. Running the
missing diagnostic would not help because the resulting operator is already
occupied. The other imports fail because their required latent variable is not
identified from the permitted pixels, labels, and transform panel.

You are the fresh independent adversarial reviewer in a rigorous method-invention
protocol. Start cold. Do not inspect the local filesystem, repository, git
history, prior conversation, or critic notes. The frozen proposal follows this
rubric in the same prompt. Use web search and primary literature.

Legal task: zero-shot image similarity learning with disjoint train/test
identities on CUB-200-2011, Cars196, In-Shop, or SOP. Only official training
images, identity labels, ordinary augmentation, and ImageNet-1K initialization
are permitted. Deployment must be one model, one view, one fixed 512-D global
descriptor, ordinary cosine/Euclidean nearest-neighbour retrieval.

Matched Lane-A references are ResNet-50/512-D/single-view: PFML CVPR 2025 CUB
0.734 +/- 0.003, Cars 0.927 +/- 0.003, SOP 0.829 +/- 0.002 over five runs;
PA+DADA In-Shop 0.930 with no reported uncertainty. Do not mix capacity lanes.

Review the frozen RIM proposal without silently repairing, completing, or
replacing it. Return LIVE, DEAD, or UNRESOLVED. Independently check:

1. every definition, estimator, stop-gradient, normalization, expectation,
   gradient path, schedule, bank update, optimizer interaction, and cost;
2. whether the reliability/interaction statistic is identifiable from the
   specified views and labels, and whether its claimed extrema and uniqueness
   actually follow under normalized descriptors and the complete base loss;
3. cheapest shortcuts: constant/collapsed coordinates, augmentation encoding,
   class or instance memorization, batch composition, scale manipulation,
   bank leakage/staleness, and representation rotations;
4. closest primary prior art inside and outside DML, especially reliable
   components/correlation, ICC or generalizability theory, augmentation
   consistency and invariance, VICReg/Barlow Twins/HSIC/coding-rate methods,
   interaction information, supervised contrastive or proxy regularizers, and
   any work optimizing repeatability conditioned on class labels;
5. whether RIM is a materially new training object rather than a renamed
   conditional covariance, reliability-weighted variance, or known
   invariance/decorrelation objective;
6. whether every matched-compute control has the stated null and isolates the
   claimed mechanism from generic variance expansion, covariance suppression,
   extra views, regularization, mining, and memory-bank effects;
7. whether forecasts follow quantitatively from premises, use a reproducible
   matched baseline, and cross the correct frontier with decision-relevant
   uncertainty; and
8. hidden data, contamination, inference, tuning, selection, recipe, capacity,
   and benchmark-protocol violations.

The provider job exited nonzero but returned an untruncated proposal with a
clean ending. Judge only the frozen text. Any substantive repair is a different
proposal and must not be credited. Cite primary sources. Identify the single
strongest decisive reason and every internally inconsistent, false, or
underdefined operation.

--- FROZEN PROPOSAL BEGINS ---

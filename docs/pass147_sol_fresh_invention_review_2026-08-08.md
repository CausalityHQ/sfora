# Pass 147 — independent Sol invention review (2026-08-08)

## Status

`NONE` at Gate 2.  This was a fresh read-only proposer pass using Codex Sol
because Fable and its Claude fallback are at the weekly usage limit.  It did
not edit files or use the GPU.  The active Pass133 CIS dropout run is therefore
unrelated evidence and remains in progress.

## Evidence boundary supplied

The review was given the corrected In-Shop reference (`raw-best 0.91815 ±
0.00126`, `frozen-final 0.91539 ± 0.00132`), CUB HIST `0.7082` and PA `0.6919`,
the current benchmark horizon, and the repository's audited closures of EMA,
contextual/neighbourhood, rival-signature, multi-centre/ECOC, graph/OT and pair
weighting, synthetic transport, JEPA/distillation, gradient/optimizer,
activation/architecture, and CIS coalition routes.  It was asked for exactly
one concrete train-time object with a measured provenance, primary-source
collision audit, CPU diagnostic, frozen In-Shop threshold, matched controls,
and an out-of-sample plan.

## Result

The review found no defensible object that clears Gate 2.  The measured signals
(17.94% opposing same-class gradients, roughly 40% fragmented training-class
graphs, centroid/proxy disagreement, and stable cross-seed errors) motivate
occupied gradient-surgery/assessment, graph or neighbourhood learning,
multi-centre, mining, or weighting mechanisms.  In-Shop training retrieval is
already about `0.9955`, so fragmentation is not evidence of underfitting, and
the transport diagnostic (`rho_32 ≈ 0.93`, registered requirement `≥ 1.15`)
rejects the proposed transferable-variation premise.

The only remaining escape is a new pixel-derived, class-disjoint transferable
signal.  It has not been measured.  The registered CPU gate is:

* cross-fitted held-out-identity `Delta AUC >= 0.05`;
* bootstrap lower bound above zero and at least 4/5 positive folds;
* cross-acquisition `Delta AUC >= 0.05`;
* cross-seed Spearman `>= 0.50`.

Inventing a training objective before that diagnostic passes would violate Gate
1.  No GPU candidate, forecast, or SOTA claim is authorized from this pass.

## Interpretation

This is a bounded negative, not proof that no future method exists.  The next
scientifically justified action is to measure the missing pixel-derived signal
with the registered identity-disjoint diagnostic.  If it passes, generate a
new frozen proposal from that measurement; if it fails, record the result and
do not spend GPU on an unsupported mechanism.

# Pass 143 — cross-field invention search (NONE at Gate 2)

Date: 2026-08-08

Codex Sol reviewed the repository measurements and prior-art ledger for a new
train-time similarity method across supervision, architecture, geometry,
optimization, sampling, and cross-field mechanisms. Fable/Claude Opus were
unavailable because of the provider weekly limit; that absence is not
scientific evidence.

No candidate survives provenance plus mechanism-level Gate 2. No CPU
diagnostic, preregistration, implementation, or GPU run is authorized.

The strongest attempted escapes were rejected as follows:

* Owner-safe coalition credit reduces to ordinary single-image proxy
  supervision for a linear sum; nonlinear/live-context escapes are covered by
  DCML (CVPR 2021) and Cross-Image Attention (CVPR 2023).
* Disjoint-identity variation transport is feature augmentation/variance
  transfer (including Meta Variance Transfer, AISTATS 2020), and the CPU test
  is adverse (`rho_32 = 0.9312, 0.9287, 0.9345`, below the locked 1.15 floor).
* Training-only trunk discrepancy feedback is occupied by AdvRF (ICCV 2025)
  and VAPNet (NeurIPS 2023).

Potential-field, multi-centre, sampling/graph, and counterfactual routes are
already occupied or failed the In-Shop screen. The search can reopen only when
a new measured information channel appears that transfers across disjoint
identities and is not reducible to labels, distances, proxies, augmentations,
peer features, teacher targets, graphs, or optimizer coefficients.

## Evidence correction

The historical In-Shop `0.9035` baseline is retracted. The valid four-seed
reference is raw-best `0.91815 ± 0.00126`, frozen-final `0.91539 ± 0.00132`.
Future screens require same-seed paired controls and must not use `0.9035`.

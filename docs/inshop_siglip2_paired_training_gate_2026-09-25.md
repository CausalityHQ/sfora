# In-Shop SigLIP2 paired training gate, 25 September 2026

The official In-Shop TRAIN partition is the only data used for training and selection. Its partition SHA-256 is `cfada103c44df866db5e2ee9ecc2301ca691a4d0cdb3c875fe4051b62570894c`. The [frozen class-disjoint split and schedules](inshop_siglip2_coverage_preflight_2026-09-25.md) contain 23,342 fit images and 2,540 held TRAIN queries. The official query and gallery remain unopened for selection.

The [pretrained SigLIP2 Large/256 TRAIN feature cache](evidence/compact_metric/sop-siglip2-substrate-v1/inshop-siglip2-train-features-v1.json) contains all 25,882 official TRAIN rows, with feature file SHA-256 `f232584491bf4ed1cf75daa7fcc03f218e7db5df797f4d57dd2bf034ac110885`. On the DGX Spark GB10 it took 216.163 s to encode. Receipt SHA-256: `da3a2cdfb3d4fc90bfb562c15c9eb0e3b1747b6547883501b596f00ef8ec6e1b`.

The first [bank cost gate](evidence/compact_metric/sop-siglip2-substrate-v1/inshop-member-bank-cost-worst-v1.json) failed: an all-anchors worst-case width of 161 positives took 79.578 ms median, above 60 ms. This is a real long-tail cost, caused by the largest product having 162 images. It is not the common batch width. Trimming only trailing `-1` padding preserves the loss and gradient; the test is in `tests/test_smooth_ap_bank_loss.py`. The [schedule-sampled cost receipt](evidence/compact_metric/sop-siglip2-substrate-v1/inshop-member-bank-cost-sampled-v2.json) times 60 rank-active batches spanning all three frozen schedules: median 8.001 ms, p95 44.293 ms, with a 60 ms median gate passed. The full 161-wide worst case remains recorded. Receipt SHA-256: `6fb8c5acb616e97cf7d739641a01ed87daa7a67fbe6374ac43c66cb1ed92d12c`.

The paired arms use the same SigLIP2 Large/256 backbone, 128-D PCA-initialized head, fit-only ArcFace proxies, BF16 vision autocast, AdamW rates and decay, 1,000 updates × 64 images, coverage-first schedule, 256-pixel augmentation, and packed 130-byte int8 scorer. The bank arm uses the unchanged SOP coefficient 8.0 and detached full-fit SmoothAP bank; the fixed float arm uses the unchanged SOP coefficient 21.93 and in-batch float SmoothAP. Both skip the rank term for an entire batch containing a singleton fit product while retaining ArcFace; bank refresh continues. The only bank speed change removes trailing invalid positive padding and is algebraically exact.

The 17-update [bank](evidence/compact_metric/sop-siglip2-substrate-v1/inshop-bank-smoke17-v1.json) and [float](evidence/compact_metric/sop-siglip2-substrate-v1/inshop-float-smoke17-v1.json) smokes both completed on seed 179023 and traversed singleton step 17. Their frozen schedules and first ten input-batch hashes match. Bank training took 22.642 s and peaked at 22.913 GB CUDA; float took 21.695 s and peaked at 21.077 GB. These are functional and resource checks, not quality results. The final float-smoke trainer source SHA-256 is `f6af548085c2c7578d1dfe059bd3348675e6a68bc668e595346a6748182ddb83`; the earlier bank-smoke source differs only in static typing/provenance edits, so a full paired run must use one identical final source.

Freeze seed order `179023`, `179024`, `179025`; run bank then float serially within each seed. Each run exports all TRAIN images at batch 64 and scores the held TRAIN queries against the full TRAIN gallery, excluding self, in packed-score arithmetic. Compare three-seed paired Recall@1 and mAP@R, report all seeds and training wall/throughput/peak VRAM. A single seed or TRAIN holdout cannot establish official In-Shop quality. Do not tune using official query/gallery. A failed source/finite/cost check stops the campaign for diagnosis; a low quality number is reported, not silently repaired or discarded. After the paired TRAIN gate, select one fixed arm by mean packed Recall@1 (mAP@R tie breaker) and read official query/gallery for all three selected checkpoints, then compare the published 96.7% UNICOM reference and matched local baselines. Numerical superiority alone is not a state-of-the-art claim.

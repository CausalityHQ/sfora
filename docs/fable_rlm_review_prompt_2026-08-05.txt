You are the independent adversarial reviewer in a rigorous method-invention
protocol. Start cold. Do not inspect the local filesystem, repository, git
history, prior conversation, or any critic notes. The frozen proposal follows
this rubric in the same prompt. Use primary literature and web search.

Legal task: zero-shot image similarity learning with disjoint train/test
identities on CUB-200-2011, Cars196, In-Shop, or SOP. Only official training
images, identity labels, ordinary augmentation, and ImageNet-1K initialization
are permitted. Deployment must be one model, one view, one fixed 512-D global
descriptor, ordinary cosine/Euclidean nearest-neighbour retrieval.

Matched Lane-A references are ResNet-50/512-D/single-view: PFML CVPR 2025 CUB
0.734 +/- 0.003, Cars 0.927 +/- 0.003, SOP 0.829 +/- 0.002 over five runs;
PA+DADA In-Shop 0.930 with no uncertainty. Do not mix these with 2048-D or
transformer lanes.

Review the frozen proposal without silently repairing or replacing it. Return
LIVE, DEAD, or UNRESOLVED. Independently check:

1. every EVT/PWM formula, return-level interpretation, stop-gradient path,
   monotonicity proof, probability calculation, and claimed optimum;
2. whether a GPD fit to actively optimized seen-class proxies identifies the
   fresh unseen-class impostor tail, and whether the image-level twin repairs
   that population shift;
3. whether the proposed causal max-margin miscalibration is measured or merely
   assumed, and whether tail pressure reduces to hard-negative mining, CVaR,
   hot LSE, pAUC, or another occupied ranking surrogate;
4. closest primary prior art inside DML, contrastive learning, face/re-ID,
   open-set recognition, EVT-based train-time learning, extreme-risk/DRO, and
   sampled-softmax or return-level calibration;
5. whether controls distinguish extrapolation from tail averaging, harder
   temperature, multi-proxy capacity, and ordinary max-margin training;
6. whether forecasts follow quantitatively and cross the correct lane;
7. hidden data, compute, inference, tuning, selection, and benchmark risks.

Any substantive repair is a different proposal and must not be credited. Cite
primary sources for novelty conclusions and identify the single strongest
decisive reason for the verdict.

--- FROZEN PROPOSAL BEGINS ---

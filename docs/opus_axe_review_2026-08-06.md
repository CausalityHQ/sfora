Verdict sent to the operator. Summary of the adjudication:

**DEAD** — earliest failed gate: the certificate gate (Gate 3 in the ladder I reconstructed and stated; if the program orders the corrected-In-Shop-first paired screen ahead of theory, that gate fails first instead, with the same outcome).

The decisive reason is a currency mismatch: everything AXE optimizes and measures is the descriptor's linear rank, while the error mode it claims to fix is driven by how many visual attributes get computed. Those are separable, and I separated them numerically — holding the computed attribute set fixed at 3, a nonlinear recoding takes class-mean rank from 3 to 99, the participation ratio from 2.8 to 10.6 (against F7's +30% threshold), and the excision loss from 0.108 to 0.008.

Three independent findings each carry weight on their own: Proposition 2's conclusion is generically true at random initialization for every pre-registered (dataset, *k*) pair, so the vacuity the proposal concedes for *K*>1 applies verbatim to the arm it calls scientifically clean; F6 cannot detect evasion (~9×10⁻⁵ over 64 draws in a 22,272-dimensional Grassmannian); and four of the nine falsification thresholds sit below the design's own minimum detectable effect.

I kept the correct parts separate from the verdict — the restricted-cosine formula and both analytic gradients verify exactly, Props. 1, 3 and 4 are sound, Prop. 2's inequality is true and conservative, and the class-disjoint selection protocol and ambiguity ledger are the strongest parts of the object. The empirical question (does a rotation-invariant excision term beat matched PA and beat C2) is not refuted by anything I found, but pursuing it would be a new proposal.

# SOP true lower-stack freeze transfer gate, 26 September 2026

In-Shop seed 179024 showed that freezing SigLIP2 Large/256 patch/position embeddings and vision blocks 0–11 reduced training wall and VRAM materially while packed top-1 differed from full control by one query and mAP@R rose. The frozen In-Shop point-estimate gate failed, so this is a distinct **SOP transfer test**, not a retry or promotion of that result. The asymmetric In-Shop diagnostic failed its own usefulness threshold and has no selection authority.

The SOP comparison uses official **TRAIN only**: 53,700 fit images in 10,186 products and 5,851 held query images in 1,132 unseen products. The evaluation gallery contains all 59,551 TRAIN images, self excluded, as in the existing SOP trainer. It differs substantially from official TEST and has informed prior selection. Use seed 179024, SigLIP2 Large/256, the same 128-D head, ArcFace plus detached full-fit member-bank SmoothAP coefficient 8, BF16 vision, coverage-first identity batches, 1,000 updates, identical optimizer/augmentation, native exact 130-byte packed scorer and one immutable source checkout. Run a serial control and `--freeze-lower-stack` treatment from the same source, preflight, feature cache, model and schedule. The flag freezes only vision embeddings and blocks 0–11; upper blocks, pooling, post-layer norm, head and classifier remain trainable.

Frozen gate before new quality results:

1. Run 17-update control and treatment smokes serially on DGX Spark GB10. Require identical first-ten input, schedule, model, cache, PCA, source and split hashes, finite losses and optimizer steps, and treatment checkpoint embedding/lower-block tensors bit-identical to the pinned pretrained model while upper/head/classifier tensors change. Advance only if treatment training wall and peak allocated CUDA are each at least **10% below** the paired smoke control. The older coverage-first receipt is context, not a same-source control.
2. If smoke passes, run exactly one serial 1,000-update control/treatment pair and evaluate the unchanged TRAIN holdout with exact native packed-score parity. Advance only if treatment packed Recall@1 point is at least control, the paired product-bootstrap 95% lower bound is greater than **−0.5 percentage points**, mAP@R is at least control, training wall including bank initialization is at least **15% lower**, and peak allocated CUDA at least **20% lower**. Require complete finite steps, checkpoint/source/resource hashes and per-query vectors. If any gate fails, stop before further seeds and official TEST.
3. A passing seed remains exploratory. Freeze a seed-aware independent confirmation protocol on fresh seeds 179026 and 179027 before considering the training option for production. Serving storage, scorer and image-to-top-k path are unchanged by construction; measured serving latency is a separate gate.

Historical seed-179024 coverage-first bank context (older trainer source) was **92.3090% packed Recall@1**, **0.767332 mAP@R**, **1,150.388 s** per 64,000 sampled images and **21.409 GB** peak allocated CUDA. It is not substituted for the new same-source control. No official or SOTA inference follows from this TRAIN-only screen.

The serial [same-source control smoke](evidence/compact_metric/sop-siglip2-substrate-v1/sop-true-freeze-smoke-179024/control.json) and [true-freeze smoke](evidence/compact_metric/sop-siglip2-substrate-v1/sop-true-freeze-smoke-179024/freeze.json) receipts have SHA-256 `2e6247fccddcda53a8ba01a2a717a74b546ca46bf7a0918bfe793878a2124b65` and `cca122358124f327892ac812435f5be4d699b431b1439c66a70b35986e47ad82`; the [terminal journal](evidence/compact_metric/sop-siglip2-substrate-v1/sop-true-freeze-smoke-179024/journal.log) has SHA-256 `4d5995e6ea93442d2ff9c5bb80129a627e0e385510ff6b3ed50dc82892c58b44`. The paired runs had identical first-ten input, schedule, model, source, PCA, initial head/classifier, and query hashes; both had finite 17-step loss and identical first loss. The treatment left all **3 embedding and 192 lower-block** tensors bit-identical to the pinned pretrained model, while the control changed all 195; all **192 upper-block**, two head and classifier tensors changed across arms. Wall including bank initialization fell from **21.970 to 14.907 s** (**−32.1%**), and peak allocated CUDA from **21.409 to 11.794 GB** (**−44.9%**). The frozen smoke gate passes; retrieval quality remains unmeasured at this stage.

The [same-source full control](evidence/compact_metric/sop-siglip2-substrate-v1/sop-true-freeze-full-179024/control.json) and [true-freeze treatment](evidence/compact_metric/sop-siglip2-substrate-v1/sop-true-freeze-full-179024/freeze.json) receipts have SHA-256 `1bc737d77ea19b40b74ca6ca967923696403c656c42c17930ace732f8ca0965f` and `07b4716b42d1291b9c195774ebd48d9df89a3b578ee54efdb94662c5e125d1c5`. The [frozen decision](evidence/compact_metric/sop-siglip2-substrate-v1/sop-true-freeze-full-179024/decision.json) and [terminal journal](evidence/compact_metric/sop-siglip2-substrate-v1/sop-true-freeze-full-179024/journal.log) have SHA-256 `31ebbe4ae71c5f1c7922ef9140721957ccc408eab165df1570678a8843988ed5` and `df6f6ce35f679a4d136e360c1b564addf5bd2be14752526549c9c91699c0d1e1`. Packed Recall@1 rose from **92.3090% to 92.8217%** (paired +0.5127 percentage points, product-bootstrap 95% CI +0.0672 to +0.9746), mAP@R from **0.767332 to 0.777476**, training wall including bank initialization fell from **1,153.662 to 752.534 s** per 64,000 sampled images, and peak allocated CUDA from **21.409 to 11.795 GB**. The frozen quality and cost gates pass. This one TRAIN-only seed is exploratory and does not authorize a production default or SOTA claim.

Fresh-seed confirmation is frozen before observing seed 179026: run paired full control then freeze for seed **179026**, analyze with the same source-bound analyzer and identical seed-specific inputs, and only then run seed **179027** in the same order. Each seed independently must satisfy every full-pair gate in step 2, including nonnegative packed Recall@1 point, paired product-bootstrap lower bound greater than −0.5 percentage points, nonnegative mAP@R point, 15% training-wall and 20% peak-CUDA savings, finite 1,000-step receipts, complete hashes, and native exact scorer parity. Stop immediately on any failure. If both pass, retain freeze as an opt-in training option pending separately frozen official-quality and serving-latency validation; no official TEST read is authorized by this TRAIN-only gate.

Fresh seed **179026** passed that same gate. The [control](evidence/compact_metric/sop-siglip2-substrate-v1/sop-true-freeze-full-179026/control.json) and [freeze](evidence/compact_metric/sop-siglip2-substrate-v1/sop-true-freeze-full-179026/freeze.json) receipts have SHA-256 `c69de5e542d2d919de5875b4313904efafd8d03d5a0271a0bc0199b45dbd14e0` and `a3153e1d2429560ea5902bd68d3e43bee17bd0509330a581a48b40e6a824ea8a`. The [decision](evidence/compact_metric/sop-siglip2-substrate-v1/sop-true-freeze-full-179026/decision.json) and [journal](evidence/compact_metric/sop-siglip2-substrate-v1/sop-true-freeze-full-179026/journal.log) have SHA-256 `e12af7bc484e140e1bde577ff713c0eee711fa6854ec8babbf0607afa74cf364` and `94f25e5ca99d23efd741e3f9a1e63d3eb4e93e1e21e57d909184c963b230f042`. Packed Recall@1 rose from **91.9843% to 92.5312%** (paired +0.5469 percentage points, product-bootstrap 95% CI +0.0342 to +1.0381), mAP@R from **0.763144 to 0.775624**, training wall including bank initialization fell from **1,148.999 to 752.865 s** per 64,000 sampled images, and peak CUDA from **21.409 to 11.795 GB**. All source, schedule, finite-step, resource, and exact native scorer checks passed. Seed 179027 is the last TRAIN-only confirmation; official TEST remains unopened for this option.

# Pass 90 — information-theoretic review (2026-08-07)

## Coding-rate / MDL objective — dead at Gate 2

The candidate would add a class-conditional coding-rate term to Proxy Anchor,
using the observed unseen-transfer gap as motivation for preserving a richer
within-class code. MCR² (Yu et al., NeurIPS 2020) already maximizes coding-rate
reduction between the whole sample set and class partitions. Anti-Collapse Loss
for Deep Metric Learning Based on Coding Rate Metric (2024) directly applies a
coding-rate metric to DML. Minimum-description-length and information-theoretic
metric learning are also established. A new coefficient or batch estimator
would be an occupied regularizer, not a novel method. No GPU run.

## Result

No candidate cleared Gate 2. The DGX remains idle.

# In-Shop Proxy Anchor reference repair 244

Date: 2026-08-03. Written before the corrected run. The active official-SOP run
retains GPU priority; this registration authorizes no preemption.

## Why the old control is invalid as an official threshold

The historical three-seed In-Shop Proxy Anchor mean 0.9035 was generated by a
shared modified harness that (1) kept the incomplete final batch, (2) froze the
new BN-Inception embedding head during epoch-1 warm-up, and (3) unit-normalized
proxy parameters instead of using upstream raw Kaiming-normal initialization.
Paired candidate failures under that shared harness remain observations, but
0.9035 cannot serve as an exact official-recipe control for future Gate-4 runs.

## Locked run

Run seed 0 with exact recipe `proxy_anchor.inshop.official-51db570`, now including
`drop_last_train_batch=True` and `proxy_initialization=kaiming_normal`. The
material upstream settings are original official train/query/gallery partitions,
BN-Inception GAP+GMP at 512 dimensions, batch 180, AdamW weight decay `1e-4`,
learning rate `6e-4` for backbone and embedding head, proxies at 100x, one
warm-up epoch with head and proxies trainable, trainable BatchNorm, StepLR 20 /
gamma 0.25, 60 epochs, and gradient value clipping at 10. Save the final model
and independently reconstruct final-state query-to-gallery retrieval.

## Prediction and falsification

The upstream improved recipe reports In-Shop R@1 **0.919**. Predict seed-0
best-over-training R@1 in **[0.904, 0.934]**. Outside that interval blocks method
screening and triggers another fidelity diagnosis; it is not a method result.
Report code-derived best-over-training, selection-corrected, and independently
recomputed final-state R@1 separately. One seed repairs the control's provenance
but does not estimate a stable method effect.

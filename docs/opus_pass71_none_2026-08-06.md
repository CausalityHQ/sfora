# Pass 71 — blind Fable/Claude search: NONE

Fable was out of credits and the job fell back to Claude Opus. No candidate
survived. The prompt's own gates are contradictory: a cold proposer forbidden
from reading the repository cannot also provide measured repository
provenance. Therefore Gate 1 guarantees NONE unless provenance is supplied by
a separate evidence-aware adjudication pass.

The substantive closure is stronger than Pass 70 but remains scoped. A
stop-gradient scalar surrogate can represent any batch-measurable tangent
field, so non-integrability is not a loophole by itself. Piecewise-categorical
routing/topological/VQ heads are almost-everywhere Jacobian methods or require
zeroth-order estimation; evolution strategies have variance proportional to
the 24.6M-parameter dimension and optimize the same scalar objective at vastly
higher cost. Bilevel constraints reduce to KKT penalties/hypergradients, and
static architectures either are approximation-equivalent to the existing
interface or differ only through implicit optimization bias. These are
closures, not evidence that no future method can exist; a pure weight-space
map outside projection, preconditioning, and data-dependent loss remains the
explicit residual loophole.

The corrected four-seed In-Shop PA reference gives a practical power gate:
mean frozen-final R@1 0.9153889436, SD 0.0013195712 (0.132 pt). At four seeds,
80%-power detection requires roughly +0.31 pt, so future candidates must
pre-register at least 0.9185 or use more seeds. No GPU run was authorized.

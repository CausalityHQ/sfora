Provider: fresh Fable session with configured Claude Opus fallback, independent
cold adversarial reviewer
Frozen before review consultation start: 2026-08-06 UTC

Read exactly:

  /home/rb/worktrees/sfora-emafactorial/docs/opus_nsrc_proposal_pass41_2026-08-06.md

Do not edit repository files or inspect git history, local audits, prior reviews,
the verdict ledger, or conversations. Start cold from the frozen proposal and
primary sources. Do not start or delegate to any other consultation or agent;
perform the review directly. Return LIVE, DEAD, or UNRESOLVED with the earliest
failed gate and one decisive mechanism-level reason. This is adjudication, not
repair. Do not silently improve, reinterpret, or replace the frozen object.

Problem and legal envelope: train on the official training split with identity
labels, ordinary image augmentation, and ImageNet-1K initialization only. Test
identities are disjoint. No test images, gallery statistics, external generated
data, text model, reranking, transduction, ensemble, or test-time fitting may
enter training or selection. Deployment is one single-view image encoder, one
512-D L2-normalized descriptor, and ordinary cosine nearest-neighbour retrieval.
All train-only machinery must disappear at deployment. Hyperparameters may be
selected only through a class-disjoint split of training identities.

Audited matched Lane-A references are ResNet-50/512-D/single-view PFML over five
runs: CUB 0.734 +/- 0.003, Cars196 0.927 +/- 0.003, SOP 0.829 +/- 0.002. These
published results are credible horizons but their complete authors' executable
recipe is unavailable locally. Higher-capacity single-view observations are
VAPNet ResNet-50/2048-D at CUB 0.762, Cars 0.948, In-Shop 0.939; AdvRF at CUB
0.766 and Cars 0.949; and CRT MiT-B2/128-D at In-Shop 0.9448. They have no
reported multi-seed uncertainty and belong to separate capacity lanes, but a
general SOTA claim must confront them. The corrected local In-Shop Proxy Anchor
seed 0 is raw 0.9163 and final 0.9137; one seed is not a fixed baseline. A live
candidate must first establish a same-seed corrected In-Shop paired control,
then confirm out of sample and on a second dataset. Report raw and independently
selected/final metrics; best-over-test-training is not valid selection.

Before recommending implementation or GPU work, independently:

1. Reproduce the exact projector and loss. Distinguish the 95%-proxy-energy
   eigenspace from the full algebraic proxy span. Derive the Proxy Anchor feature
   gradient through cosine normalization and project it into the specified
   complement. Check the claimed radial-null and pointwise-orthogonality
   theorems exactly as frozen, including proxy-tail directions.
2. Distinguish orthogonality of gradients with respect to a descriptor from
   orthogonality after a shared backbone/head Jacobian and adaptive optimizer.
   Audit whether this establishes zero interference in parameter training.
3. Audit the moving projector: recomputation every 50 iterations, eigenvector
   rotations, eigenvalue multiplicity, hard 95%-threshold rank changes,
   dimension changes in `c`, stop-gradient semantics, and optimizer-state/basis
   transport. Decide whether the objective is uniquely executable.
4. Audit all degeneracy claims. Evaluate `logdet(I + a C C^T)` at rank-deficient
   and constant codes; determine whether its loss is infinite. Check `c=0`,
   `d-r=0`, normalization without epsilon, the energy-floor gradient at `b=0`,
   finite-batch rank ceilings, scale games, instance lookup, background/context
   codes, and augmentation-stable nuisance.
5. Test the deployment argument. If the complement is class-exchangeable or
   carries image-instance information rather than unseen-identity information,
   derive how a forced 15% energy share changes positive and negative cosine.
   Decide whether a near-chance seen-class probe is favorable evidence or a
   warning that deployed content is retrieval-irrelevant.
6. Audit the proposed `NSRC + multi-proxy` frontier arm. With 15 proxies for
   each of 100/98 classes in 512 dimensions, determine when the exact proxy span
   is full rank and `d-r=0`; determine whether retaining a 95%-energy complement
   merely puts omitted supervised proxy-tail directions into the “null” block.
   Decide whether the theorem, nonempty complement, and combined crossing can
   hold simultaneously.
7. Search primary prior art aggressively inside and outside DML: DiVA; MIC and
   Sharing Matters; Null-Space Provisioning-like class-statistic complements;
   shared/private or orthogonally partitioned representations; Anti-Collapse
   Loss for DML; MCR-squared/coding-rate maximization; SVMax; VICReg, Barlow
   Twins and W-MSE; rho spectral regularization; S2SD; multi-proxy methods;
   neural collapse; orthogonal-gradient/multitask methods; and continual-
   learning null-space projection. Judge supervision object and action, not the
   exact live-projector wrapper.
8. Audit causal provenance. Hyperparameter counts, dimensional rank bounds,
   a future premise falsifier, and literature diagnoses are not repository
   measurements. State which corrected checkpoint measurements would be needed
   to show proxy-complement energy collapse causes official-query errors and
   that complement information transfers across unseen identities.
9. Audit C1--C6 and D1. Identify missing paired-view/no-null controls, full-span
   versus truncated-span controls, unique-image exposure changes, and whether
   any stated control can separate useful unseen-identity content from stable
   nuisance or pure regularization.
10. Check protocol compliance: corrected paired In-Shop screening must follow
    Gates 1--3, trainable BatchNorm and repository recipe must remain matched,
    and every arm needs raw-best plus independently selected/final metrics.
    Recompute FLOPs, memory, wall-clock, uncertainty, and frontier arithmetic.
    Compare standalone and combined claims separately and do not credit an
    undefined or unimplemented composition.

Preserve correct subcomponents separately from the verdict. Cite primary
sources and state unresolved uncertainty. A substantive repair is a new
proposal and must not be used to upgrade this frozen one.

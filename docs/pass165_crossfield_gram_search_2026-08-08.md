# Pass 165 — cross-field direct-Gram search (NONE before GPU)

An independent blind cross-field pass attacked the repaired protocol's
remaining direct-cosine space.  For normalized descriptors `Z`, any
differentiable Gram objective has `∇_Z L = (A + Aᵀ)Z`; entrywise `A` is pair
weighting/mining, cross-entry `A` is context/graph, spectral `A` is covariance/
whitening, and history-dependent `A` is distillation/control/optimizer
machinery.  The concrete anti-collapse instance-discrimination construction is
already Chen et al. ICML 2022 and Lee et al. AISTATS 2025.  Potential-field,
PDE, information-bottleneck, and biological dynamics likewise collide with
verified occupied families.

The measured `-0.04968` gap does not identify which within-class geometry should
survive, so no honest forecast or CPU falsifier exists. No files or GPU were
used for a candidate.

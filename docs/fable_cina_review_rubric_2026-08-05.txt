You are the fresh independent adversarial reviewer in a rigorous method-invention
protocol. Start cold. Do not inspect the local filesystem, repository, git
history, prior conversation, or critic notes. The frozen proposal follows in
this prompt. Use web search and primary literature.

Legal task: zero-shot DML with disjoint train/test identities on CUB, Cars196,
In-Shop, or SOP; official training images/labels and ordinary augmentation only.
Deployment is one ResNet-50, one view, one fixed 512-D descriptor, cosine NN.
Matched Lane-A references: PFML CUB 0.734 +/- 0.003, Cars 0.927 +/- 0.003,
SOP 0.829 +/- 0.002 over five runs; PA+DADA In-Shop 0.930, uncertainty unknown.

Review frozen CINA without repairing or replacing it. Return LIVE, DEAD, or
UNRESOLVED. Independently check:

1. every covariance estimator, denominator, split/null construction, projection,
   inverse square root, eigensolve, clamp, stop-gradient, hinge, schedule, and
   sampler is executable and differentiable as claimed;
2. whether D=0 iff proportional covariance on the actually measured subspace,
   whether affine invariance survives stopped top-k projection, and whether the
   null subtraction is unbiased or comparable to the cross-identity statistic;
3. collapse, rank-1/common-low-rank shape, isotropic noise, radial/norm coding,
   head absorption, batch/split gaming, singular covariance, repeated images,
   and classes with too few samples;
4. whether proportional class covariances are necessary/sufficient for a single
   cosine descriptor to be Bayes-optimal, and whether the proposed pooled split
   statistic identifies unseen-identity-useful nuisance rather than arbitrary
   training-image covariance;
5. exact prior art: covariance homogeneity/equality penalties, common principal
   components, Box M and affine-invariant SPD metrics, CORAL/domain alignment,
   covariance alignment in metric/re-id/face learning, MIC/DiVA/Sharing Matters,
   NIR/nivMF/HIST, IRM/REx, and differentiable covariance losses;
6. whether controls isolate shared-shape mechanism from sampler, spectral or
   covariance regularization, rank collapse, head/radial shortcuts, and extra
   hyperparameter search; verify every control's stated null;
7. whether forecasts derive from measured premises and a reproduced matched
   base, cross the correct frontier, and use valid uncertainty arithmetic; and
8. data, contamination, tuning, selection, compute, inference, recipe, capacity,
   and benchmark-protocol violations.

Any substantive repair is a new proposal. Cite primary sources. Identify the
single strongest decisive reason and every false, inconsistent, or underdefined
operation.

--- FROZEN PROPOSAL BEGINS ---

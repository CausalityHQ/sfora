# Candidate 315: topology-preserving class supervision

## Gate 1 diagnostic

The corrected In-Shop final training embedding has 3,975 classes with at least
three samples. A mutual 1-NN graph is disconnected for **39.95%** of classes,
but class fragmentation does not predict the residual sample-level failure:
foreign-nearest outranks same-class-nearest for **0.3056%** of fragmented-class
samples versus **0.5376%** of connected-class samples. The sign is opposite to
the proposed mechanism. This rejects the preregistration trigger.

**Gate 1: dead.** No implementation or GPU.

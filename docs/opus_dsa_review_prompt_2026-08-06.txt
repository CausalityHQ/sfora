You are the independent cold adversarial reviewer for a frozen similarity-learning proposal. Review only this proposal file:

  /home/rb/worktrees/sfora-emafactorial/docs/opus_dsa_proposal_pass29_2026-08-06.md

Do not inspect the repository's method-search ledger, prior proposal/review files, or any hidden failure catalogue. Do not edit files. You may use primary literature and fresh CPU calculations. The proposal was generated blind and is frozen; decide whether DSA is mathematically valid, genuinely distinct from prior art, causally identifiable, executable, and capable of supporting its forecasts before any GPU is spent.

Attack the frozen method rather than improving it. At minimum:

1. Re-derive the EMA between-identity scatter, its expectation and rank, the detached top-eigenspace projector, both renormalization gradients, and the claimed rotation equivariance. Check singular cases when a sample or proxy lies in the removed subspace and whether the stated formula needs an epsilon or changes the method.
2. Test the causal argument. Does random novel-identity contrast exchangeability justify the quadratic-form variance calculation or the asserted 4.1-point ceiling? Does flattening between-training-identity scatter imply more transferable evidence, or can class-specific nuisance/artifact codes satisfy the residual loss? Distinguish an actual measured premise from a forecast.
3. Construct explicit shortcut/counterexamples for D1-D5: correlated duplication not selected among the current top r; identity codes distributed over R+1 directions; sacrificial discriminants; proxy adaptation when both embeddings and proxies are projected; EMA/eigenbasis cycling; exact/near collapse; train-identity memorization; and the effects of hard multi-proxy assignment. Decide whether D2's claim that the only cheap solution is a flat spectrum follows.
4. Audit whether the proposed multi-proxy loss is an exact and coherent Proxy-Anchor extension, including empty proxies, same-class unassigned proxies, proxy learning-rate/weight-decay choices, and whether changing K plus the recipe confounds DSA. Verify the claimed published recipe where primary sources disclose it.
5. Search primary sources adversarially inside and outside DML for training-time removal/suppression of the most discriminative feature subspace, adversarial feature dropout, iterative/adversarial erasing, orthogonal complementary features, subspace dropout, null-space training, ensemble/diversity methods, re-identification and face-recognition feature erasure, LDA/Fisher spectral objectives, and representation redundancy under learned or data-dependent masks. Judge the supervision object/action, not just the exact wrapper or name.
6. Audit controls and falsifiers. In particular: whether random-frame and coordinate dropout are truly matched; whether post-hoc train-scatter whitening is a legal and mechanism-isolating control; whether participation ratio on held-out identities measures the claimed gain spectrum; whether SOP-vs-CUB differences isolate rank starvation; and whether the proposal forecasts any statistically defensible crossing of an existing in-scope method.
7. Recompute training time and memory, including eigendecomposition, maintaining/refreshing S, projected proxy/sample logits for K=15, autograd activations, and any host-device synchronization. Check the claim that one backbone backward makes total overhead 1.02-1.03x.
8. Give a decisive verdict: LIVE, DEAD, or BLOCKED. If DEAD, name the earliest protocol gate and the single most decisive mechanism-level reason. If BLOCKED, state exactly what no-GPU evidence resolves it. Preserve any valid mathematical or engineering subcomponents separately from the method verdict.

Be precise and candid. Cite primary sources with direct links, quote sparingly, state uncertainty, and do not launch another consultation or ask another model.

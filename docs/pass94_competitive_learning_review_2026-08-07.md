# Pass 94 — competitive-learning batch (2026-08-07)

## Self-organizing/neural-gas positive structure — dead at Gate 2

The measured premise was the persistent same-wrong identity overlap (0.6476)
and the 44.7% seen-to-unseen transfer of within-class contraction.  A proposed
competitive-learning head would maintain a class-shared neural-gas/SOM codebook
and use winner/neighbor assignments to preserve multiple within-class modes
while Proxy Anchor supplies identity separation.

The mechanism is already occupied.  *Deep Supervised Quantization by
Self-Organizing Map* (ACM Multimedia 2017) combines CNN feature learning with a
supervised SOM/codebook for image retrieval, and *Deep Learning-based
Self-Organizing Map for Content-Based Image Retrieval* replaces convolutional
processing with a learned SOM retrieval representation.  In DML terms, a
class-shared competitive codebook is also the same supervision object as
multi-proxy/sub-center/SoftTriple mode assignment.  Renaming the codebook as a
neural-gas activation does not create a new train-time object.

No implementation, diagnostic, or GPU run occurred.

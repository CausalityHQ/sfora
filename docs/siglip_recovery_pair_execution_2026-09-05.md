# Fixed recovery pair execution record

Status at initial record: **running; quality not measured**. This is not a
completed result or publication evidence. Do not start a second pair from this
record: inspect the original process and monitor receipt first.

## Original execution authority

- Source commit `2bfd8df3` (final-only runner, tests and plan).
- Original local observation session70964; DGX PGID1785618, Python PID1785619.
- Immutable source root `/home/riomus/sfora-recovery-pair-7b630dc3f15f`.
- Runner SHA256 `7b630dc3f15fec64729114e9a4a5edf70e570eb6ba4e21ae01951eaaf10fe6bb`.
- Wrapper `/home/riomus/sfora-recovery-pair-7b630dc3f15f-launch.py`, SHA256
  `b7d4262f361f99b1648b3b83356c050a4bc5a2d5e1cee5e8cd0a0fbaf85b2f46`.
- Monitor `/home/riomus/sfora-recovery-pair-monitor.py`, SHA256
  `625e17599171a3a17e48e0b82d7a54b1f76ddcded29b6665da1d0d13e6e2dcb3`.
- Log `/home/riomus/siglip-recovery-pair-7b630dc3f15f.log`.
- Terminal monitor path `/home/riomus/siglip-recovery-pair-7b630dc3f15f.monitor.json`.
- Final-only output directory `/home/riomus/siglip-recovery-pair-7b630dc3f15f`;
  expected files `pa-final.pt`, `relational-final.pt`, `pair-complete.json`.
  Their existence alone does not establish successful completion; authenticate
  original terminal status and every final receipt/checkpoint binding.

The wrapper verifies seven local source hashes before importing the runner.
It uses the durable original smoke result SHA256
`0481b835f594cbc9f910c40259a5d40c1958f236f51bbea49104cfdcaffd0344`,
fresh students/optimizers, original paired inputs,198updates/arm and no evaluation.
The immutable smoke deployment remains unchanged.

The monitor counts1355seconds prior campaign time (conservative whole-wrapper
round-up), reserves1800evaluation seconds and caps this original pair process
at18445seconds. It retains the inherited110GiB RSS, PSI0.79immediate or0.50for
three5s samples,256MiB swap growth and300s progress stops. Training separately
checks96GiB CUDA reserve and leaves checkpoint/evaluation time outside updates.
No auto-resume or automatic duplicate is authorized by the protocol.

## Pre-evaluation compatibility checks

The authenticated original seed17 receipt has `evaluation_batch_size=32`.
Quality extraction must retain32, independently of the matched speed preflight's
batch8. A RED/GREEN regression caught and repaired the initial evaluator's
batch8 mismatch before it was deployed or ran scientifically.

A second RED/GREEN source-gate check repaired a local import alias collision
between the control runner and control core. Neither evaluator defect changed
the active training source or its measurements.

The prior audit's exact SHA and per-query rankings were independently checked:
2746queries,2596correct,MAP@R0.7913744556922272; sorted unique IDs and labels49..81.
The new evaluator retains and validates every per-query ranking and must exactly
reproduce the authenticated teacher hit count and MAP before judging students.
Per-query ranking equality is separately reported, including the first differing
ordinal; it is not an additional advancement gate. This distinction was fixed
before any post-recovery evaluation: floating-point/tie ordering may change an
individual ranking without changing either preregistered aggregate. Aggregate
drift still fails closed, with no numerical tolerance added. Successful
engineering advancement is not SOTA.

## Evaluator review disposition (before scientific evaluation)

Claude consultation `b5d11e9d1aa64922` reviewed the separate evaluator without
running DGX inference. The following corrections and clarifications are covered
by local tests; the active training deployment is unchanged:

- Bind quality batch32 to the authenticated original receipt, retaining the
  same128-image chunk partition. Batch8 belongs only to the speed preflight.
- Authenticate both final checkpoint byte streams and tensor states before
  decoding evaluation images. Verify the audit's RGB digest by inverting its
  nonidentity common-to-native permutation; sorted common IDs alone are not a
  decoded-pixel proof.
- Reproduce teacher aggregates immediately after teacher extraction, before
  either student gallery is embedded; preserve the explicit ranking-drift flags.
- Bind evaluator runner, evaluation-core and retrieval-core source SHA256s in
  the result. The retrieval helper is from a pre-existing dirty Sfora file, so
  a commit ID alone cannot identify its bytes. Deployment must preserve and
  authenticate that exact overlay rather than silently claiming clean-commit
  reproducibility.
- Record CPU search as an identical-width regression guard, not a search-cost
  saving. The architecture speed receipt is explicitly pre-recovery evidence;
  it is not a measurement of the final trained weights.
- Charge whole original monitor elapsed time and conservative prior1355seconds
  to the six-hour campaign. The monitor is programmatic, not a manually filled
  time receipt. Its exact source is retained at
  `docs/research-artifacts/siglip-recovery-pair-monitor-625e1759.py.txt`, with the
  SHA256 listed above.

Local evaluator gate:13 tests passed, including real reduced-model checkpoint
restoration, full2746-row retrieval and100 measured CPU-search samples. No
scientific quality conclusion follows from these synthetic tests.

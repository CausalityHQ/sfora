# Blind SOP outcome decision matrix 279

Date: 2026-08-03. Recorded while corrected official SOP Proxy Anchor was at
epoch 34, before its final report, checkpoint, embeddings, or diagnostics
existed.

## Purpose

The corrected SOP run can repair benchmark provenance and test registered
measurements. It must not become a source of post-hoc method stories merely
because one number is large. This document fixes the interpretation of every
planned output before results are known.

## Outcome map

| Output | Outcome | Permitted conclusion | Method consequence |
| --- | --- | --- | --- |
| Raw best-test R@1 | Inside registered `[0.777, 0.807]` | Local official-recipe reproduction is plausible | None; test-selected reference only |
| Raw best-test R@1 | Outside interval | Fidelity remains broken | Diagnose benchmark; no candidate comparison |
| Independent final R@1 | Any value | Frozen final-state reference and raw-to-final gap | Report both; no “selection correction” and no stability method |
| Joint artifact audit | Fails | Result inadmissible | Repair evidence path; no scientific interpretation |
| Fragmentation fraction/effect | Registered pass | Product-instance fragmentation marker replicates observationally | No connectivity repair, gating, diversity, class-shell, or multi-centre arm |
| Fragmentation fraction/effect | Effect `<=0` | Cross-dataset marker falsified | Close fragmentation-derived supervision |
| Fragmentation fraction/effect | Effect `(0,+1)` or insufficient support | Inconclusive | No candidate |
| Proxy radial clock | Registered pass | Exposure correlates with an unintended angular optimizer clock | Record behavior only; weight normalization and spherical/Riemannian optimization occupy correction |
| Proxy radial clock | Fails | Active-clock prediction falsified | Close that line |
| Nearest-negative superclass enrichment | Any magnitude | Quantifies official SOP hierarchy in learned geometry | Hierarchical DML/proxies and candidate sampling occupy intervention |

The final raw-to-final gap cannot revive the withdrawn leave-neighbour
selection-bias estimator: trajectory curvature and slope confound it. Test-curve
timing cannot revive proxy-equilibrium stopping because no training-only proxy
trajectory was recorded prospectively.

## Reopening condition

A candidate may reopen only if the verified artifacts expose a **new measured
observable not represented in the table** and that observable motivates a new
supervision object after primary-source review. A surprising value, interaction,
or correlation among the planned diagnostics is exploratory evidence and needs
an independently preregistered replication before method design.

If the artifacts contain only the planned outputs, the correct scientific
conclusion is that SOP improves the negative measurement catalogue but supplies
no novel method under the data-only, roughly-1x constraint. In-Shop may still
run as the separately registered reference after CPU serialization; it does not
change this blind SOP interpretation.

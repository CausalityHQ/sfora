# SOP true-freeze official and serving validation, frozen before TEST read

The three [TRAIN-only paired gates](sop_true_freeze_gate_2026-09-26.md) passed for training seeds 179024, 179026 and 179027. This protocol evaluates the **six existing checkpoints once**. No retraining, seed selection, ensembling, checkpoint swapping, or further loss tuning is permitted. SOP official TEST has already been read by earlier Sfora work, so these results remain exploratory even if the gate passes. DeepFashion In-Shop quality remains a separate failed gate.

| Seed | Paired decision SHA-256 | Control receipt SHA-256 | Freeze receipt SHA-256 |
| --- | --- | --- | --- |
| 179024 | `31ebbe4ae71c5f1c7922ef9140721957ccc408eab165df1570678a8843988ed5` | `1bc737d77ea19b40b74ca6ca967923696403c656c42c17930ace732f8ca0965f` | `07b4716b42d1291b9c195774ebd48d9df89a3b578ee54efdb94662c5e125d1c5` |
| 179026 | `e12af7bc484e140e1bde577ff713c0eee711fa6854ec8babbf0607afa74cf364` | `c69de5e542d2d919de5875b4313904efafd8d03d5a0271a0bc0199b45dbd14e0` | `a3153e1d2429560ea5902bd68d3e43bee17bd0509330a581a48b40e6a824ea8a` |
| 179027 | `20d0e3aeaeb24093cf05cd87feb692c878f28bac85c9abd88f2bba5fde2ca894` | `35b7f82ccfa44adf9bfb58c02711d017dea88a49e7b8e859b9437458a9fa8c4a` | `9fe8e88615ebf653cb9f0ca890b7eac472d84297fbf78370ed3b1cc5bf77b633` |

The official inventory is the pinned **60,502-image, 11,316-product TEST** split. Every image queries the full TEST gallery with self excluded. The test-image manifest SHA-256 is `28a3ec0561cd83ee426f3d1c301c70799316af91c1e9083a5a1ffdf3414327c1`; the source archive SHA-256 is `1ba27b2d6b9db39067aa6facd0ef8aafc303c4527f6feabed859b0512c7d921a`. Use the already pinned SigLIP2 Large/256 snapshot, unchanged 128-D head and 130-byte int8-plus-norm packing, and native scorer SHA-256 `39602d0e4e8b0d5ec441be460ad7f18e288241bef19fb6e6c5df14f4033ac73c`. Export uses FP32 model parameters with FP16 autocast, as in TRAIN evaluation. Record per-query packed Recall@1 and AP@R, full Recall@K/mAP@R, exact native top-10 and R1 parity, image, source, checkpoint and runtime hashes, export/score wall, and resource use. Run each control immediately before its freeze pair on the existing DGX Spark GB10, serially.

The **exploratory official-quality gate** carries forward the TRAIN-only per-seed rule: freeze packed Recall@1 and mAP@R point estimates must each be at least their same-seed control; the paired product-bootstrap 95% lower bound for Recall@1 must be greater than −0.5 percentage points for each seed. Report the three-seed mean and seed range separately; the product bootstrap conditions on the reused TEST product set and does not measure training-seed uncertainty. If any pair fails, retain the training flag as a documented SOP TRAIN-cost option but do not promote its checkpoint or use further TEST reads to tune it. Passing permits only an exploratory official-quality comparison, not a current SOTA claim. The dated published UNICOM 91.2% Recall@1 SOP value is a reference, not a matched local control.

The **serving gate** uses the same six checkpoints and an interleaved matched image-to-top-10 harness. First require the public loader at `fp32_autocast` to reproduce the offline gallery bytes and exact packed top-10 on fixed TRAIN query images; separately measure the default `fp16_native` query path against the intended offline gallery and report any code/rank/quality difference. Do not inherit offline quality for a mismatched precision path. Then measure batch 1 and 32 decode-to-result latency, throughput, peak memory and cold start against the paired full-train checkpoint with identical hardware, images, gallery, scorer, precision and arm order. Report p50/p95 as diagnostics; any p99 certification needs at least 10,000 calls per cell and the repository's paired-block bootstrap gate. Freezing only changes training, so serving speed is expected to be similar and is not inferred from training wall time. A provenance, parity or quality failure blocks checkpoint promotion; a serving failure blocks that deployed precision configuration.

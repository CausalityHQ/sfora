You are the independent cold adversarial reviewer for a frozen similarity-learning proposal. Review only this proposal file:

  /home/rb/worktrees/sfora-emafactorial/docs/opus_xtail_proposal_pass31_2026-08-06.md

Do not inspect the repository's method-search ledger, prior proposal/review files, or any hidden failure catalogue. Do not edit files. You may use primary literature and fresh CPU calculations. The proposal was generated blind and is frozen; decide whether XTail is mathematically valid, genuinely distinct from prior art, causally identifiable, executable, and capable of supporting its forecasts before any GPU is spent.

Attack the frozen method rather than improving it. At minimum:

1. Re-derive the peaks-over-threshold/GPD mathematics and the exact Hosking-Wallis probability-weighted-moment estimator under the proposal's survival-function convention. Check the definitions and order-statistic weights for `a`, `b`, raw shape, scale `(1-xi)a`, extrapolation factor `A(R,xi)`, the xi-to-zero Taylor branch, all stated numerical ranges, and every displayed gradient. Use symbolic or numerical counterexamples where useful.
2. Audit whether an upper tail of cosine similarities in a learned, bounded `[-1,1]` space can be extrapolated from about 36 per-anchor exceedances to probabilities 41x--417x deeper. Check endpoint constraints, threshold stability, iid/exchangeability assumptions, dependence from a shared anchor and repeated classes, mixtures over negative identities, nonstationarity during training, clipping `xi` to `[-0.5,0.15]`, shrinkage through an EMA, and the `min(1, q)` saturation/gradient. Determine whether the estimator meaningfully identifies tail shape or mostly converts top-quartile spread into a high-gain margin.
3. Re-derive the claimed R@1 causal connection. Retrieval error depends jointly on the maximum negative and maximum/available positive distribution, gallery class multiplicities, and query/gallery protocol. Check whether `n_target=N_train` is a legal and justified proxy for each benchmark's actual number of candidate negatives, whether the stated extrapolation factors and sample counts are correct, and whether controlling a train-identity mixture tail predicts unseen-identity retrieval rather than seen-class hardness or batch composition.
4. Attack the degeneracy proofs. In particular check collapse at the full normalized embedding (including sphere-Jacobian stationarity), the sign and optimization consequences of the threshold term, whether the inequality in D3 implies minimizers behave as claimed, whether `a->0` is a desirable geometric optimum or a collapsed/quantized tail, whether the PWM denominator and hard clips create zero-gradient or discontinuous shortcuts, whether q=1 makes the loss infeasible, and whether the positive softmax permits chaining or class fragmentation.
5. Determine by algebra and fresh simulations whether XTail is distinguishable from a tuned hard-negative/quantile/ranked-list loss. Hold tail location, order statistics, and constant `A` controls carefully. Quantify estimator variance and bias at k about 36 for bounded, Gaussian-mixture, beta, and GPD-like similarity laws. Test whether the per-anchor shape estimate or its gradient contains stable information beyond top-k mean, threshold, maximum, and a constant coefficient. Treat A4 and A7 as necessary but ask whether they are sufficient causal controls.
6. Search primary sources adversarially inside and outside DML. Inspect AnchorFace, OneFace, UniTSFace, TAR@FAR/low-FAR face losses, Recall@k surrogates, Smooth-AP, XBM, hard-negative and quantile/ranked-list losses, DRO/CVaR/top-k risk, differentiable sorting/ranking, EVT-trained embeddings, OpenMax/EVM/open-set recognition, ViEVL, extreme-quantile regression, and any work differentiating through tail estimators or fitting Weibull/GPD tails during representation learning. Judge the supervision object and optimization action, not just the exact PWM wrapper or use of `N_train`.
7. Audit the executable recipe and controls. Check sampler feasibility on CUB/Cars/In-Shop/SOP, class counts and images per class, Proxy-Anchor reduction, multi-proxy/PFML stand-in, BN freezing, 200-epoch recipe, the EMA's autograd/state semantics, AMP/sorting behavior, held-out-class model selection, seed/power plan, and whether any test-conditioned gallery-size or shape diagnostic leaks into tuning.
8. Recompute compute and memory costs, including an already-required pairwise Gram, per-row sorting, autograd through sort/PWM, retained activations, fp32 islands, and optimizer effects. Audit all forecast arithmetic and whether a predicted +0.001 SOP tie or an uncertainty-free +0.004 In-Shop comparison can satisfy the standing objective.
9. Give a decisive verdict: LIVE, DEAD, or BLOCKED. If DEAD, name the earliest failure and the single most decisive mechanism-level reason. If BLOCKED, state exactly what no-GPU evidence resolves it. Preserve any valid mathematical, statistical, or engineering subcomponents separately from the method verdict.

Be precise and candid. Cite primary sources with direct links, quote sparingly, state uncertainty, and do not launch another consultation or ask another model.

# Pass 166 — cross-dataset error-ratio search (NONE before GPU)

The reopened evidence invalidates the old universal balanced-error closure:
CUB is near 1:1 local/between error while corrected In-Shop is 3.05–3.22:1.
That establishes dataset-dependent headroom, but not which geometry to change.

Primary-art review found every direct normalized-Gram response occupied: entrywise
gradients are General Pair Weighting/mining; cross-entry dependence is context/
graph; spectral terms are scatter/whitening. Global alternatives are OLE,
MCR², and SCoRe; adversarial non-Gram routes are DAML/synthesis. No candidate
could receive an honest four-seed forecast above the useful-power floor
`0.9185` against `0.9153889 ± 0.0013196`, so no CPU or GPU work was authorized.

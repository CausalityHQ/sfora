# Corrected In-Shop training-geometry discovery plan

Date: 2026-08-03. Written while the corrected reference was still training and
before its final training embeddings existed.

The wrong-pixel retraction removes every In-Shop measurement that previously
motivated RSPG, ARCG, fragmentation, and one-seed screening. Rather than transfer
those conclusions to the new corpus, export the corrected reference's explicitly
final training state and rebuild a training-only failure atlas.

The export must be bound to the report's exact semantic configuration, final
resolved step, checkpoint/report hashes, official 25,882-row partition, finite
unit embeddings, and locked source-content profile. It may support hypothesis
generation, but no test-set score or threshold may be used to choose a mechanism.

The first atlas will recompute, without tuning:

1. leave-one-out training R@1 and true-class versus nearest-foreign margins;
2. symmetrized within-class 1-NN fragmentation, with class-size-stratified error;
3. positive-pair distance and proxy-margin distributions;
4. filename view-role transition counts only as a dataset descriptor, not a
   candidate, because view-aware supervision is already occupied;
5. the fixed RSPG signature-retention operator only as a falsification of the old
   wrong-corpus measurement, never as permission to revive RSPG.

A candidate requires a new observable outside the blind matrix and must still pass
primary-source Gate 2. The independently generated chemistry/biology batch produced
no current survivor: transition-state exclusion is adjacent to LoOp/interpolation;
response-frame chirality is augmentation-equivariance; collective exclusion is set
mining; ternary witnesses are hypergraphs/mining; irreversible consolidation lacks
stored trajectories and collapses toward curriculum learning.

You are the independent cold adversarial reviewer for a frozen similarity-learning proposal. Review only this proposal file:

  /home/rb/worktrees/sfora-emafactorial/docs/opus_nsp_proposal_pass30_2026-08-06.md

Do not inspect the repository's method-search ledger, prior proposal/review files, or any hidden failure catalogue. Do not edit files. You may use primary literature and fresh CPU calculations. The proposal was generated blind and is frozen; decide whether NSP is mathematically valid, genuinely distinct from prior art, causally identifiable, executable, and capable of supporting its forecasts before any GPU is spent.

Attack the frozen method rather than improving it. At minimum:

1. Re-derive the claim that any identity-labelled loss shapes embeddings only in the centered class-centroid span and hence leaves d-C+1 directions without identity-relevant gradient. Treat proxy, pair/tuple, normalized-cosine, nonlinear encoder, sample-specific gradient, and within-class terms separately. Construct explicit counterexamples if the C-1 conclusion is only a rank identity for centroid scatter rather than an optimization constraint.
2. Audit the online EMA-centroid projector. Check initialization, row normalization before centering, incomplete/stale class updates, the 99%-energy rank rule, SVD ties/cycling, detached-Q dynamics, the claims r≈97-99 on CUB/Cars and r→d on SOP/In-Shop, and whether Q really represents a label-null rather than merely centroid-orthogonal subspace.
3. Re-derive every gradient claim for the epsilon-normalized u and detached-denominator energy floor, including v=0 and near-zero singular cases. Determine whether feature-space orthogonality of an instantaneous z-gradient implies noninterference after the shared nonlinear backbone and optimizer update, and whether changing Q over time invalidates “structurally incapable of rotating” the supervised subspace.
4. Attack the provisioning degeneracy arguments. Check whether NT-Xent's Welch bound is a lower bound on effective learned rank or merely a bound on an unattained global spherical-code optimum; whether same-class negatives imply E[u|y]=0; whether images can be memorized by augmentation-stable instance/background/crop/JPEG cues; whether energy can be met by head scaling or moving the P/Q split; and whether allocating 25% deployed energy to instance discrimination can mechanically dilute cosine identity signal.
5. Search primary sources adversarially inside and outside DML for supervised metric learning plus self-supervision, DiVA and related embedding partitions, S2SD, BIER/A-BIER, orthogonal task subspaces, gradient projection/surgery, supervised-contrastive variants with false-negative handling, null-space or residual feature learning, subspace/feature disentanglement, redundancy reduction, neural-collapse rank remedies, and multi-task learning in learned orthogonal complements. Judge the supervision object/action, not just the exact online-SVD wrapper or energy hinge.
6. Audit the stated Proxy-Anchor reduction and recipe, the invented multi-proxy stand-in, and all controls. In particular, decide whether C3/C4/C7 isolate the claimed mechanism, whether compute matching 200 NSP epochs to 266 base epochs is valid, and whether a method forecasting below the frontier and only conditionally adding to an unreproduced PFML can satisfy the stated objective.
7. Recompute the claimed time and memory, including the second branch, separate BN, retained autograd activations, Cx512 table, full/thin SVD, all-pairs NT-Xent, and extra augmentation pipeline. Check benchmark legality and the proposed test-time probes for held-out-identity/test-conditioned tuning leakage.
8. Give a decisive verdict: LIVE, DEAD, or BLOCKED. If DEAD, name the earliest protocol gate and the single most decisive mechanism-level reason. If BLOCKED, state exactly what no-GPU evidence resolves it. Preserve any valid mathematical or engineering subcomponents separately from the method verdict.

Be precise and candid. Cite primary sources with direct links, quote sparingly, state uncertainty, and do not launch another consultation or ask another model.

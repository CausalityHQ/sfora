# Reopened candidate audit after the protocol repair (2026-08-08)

The protocol defect invalidated blind-proposer `NONE` responses, not concrete
methods that already had evidence-aware Gate 1/2 decisions. I rechecked the
reopened material against the corrected four-seed In-Shop reference
`0.9153889436 ± 0.0013195712`.

| reopened item | repaired decision |
|---|---|
| Pass 58 EVT-tail | Blind `NONE` is non-evidence; the concrete tail family remains closed by the adverse XTail transport measurement and Pass 167's PCD/tail audit. |
| Pass 59 channel recycling | Non-evidence as a blind search; no candidate-specific provenance or falsifier, and the concrete mechanism is adjacent to ReDo/continual backprop. |
| Passes 60–61 equivariance | Non-evidence as blind searches; augmentation-equivariant training is occupied by transformed-attention consistency and equivariant representation learning. |
| Pass 64/66 alpha/PSE probes | Diagnostics only, not falsely rejected training interventions. |
| Pass 67 drift budget | Bound/detector proposal, not a training object; non-evidence. |
| Pass 69 confusion/label merging | Concrete family occupied by Confusion-Based Metric Learning (Chen et al., TNNLS 2022), Group Loss, and hierarchical label propagation; dead at Gate 2. |
| Passes 70–72 | Fixed-descriptor diagnostics and process critiques, not benchmark experiments. |

## Fresh Gate-2 batch

1. **Augmentation-Jacobian cross-instance transport:** make two same-class
images agree on the finite-difference Jacobian of the deployed descriptor under
a controlled augmentation while retaining descriptor differences. This is
`LIVE-NARROW` pending primary-art comparison against transformed-attention
consistency (Li et al. 2020), AugSelf/equivariance, and class-pose decomposition.

2. **Confusion-graph label elasticity:** merge labels along a train-derived
confusion graph and supervise both partitions. `DEAD` at Gate 2 (CML, Group
Loss, and hierarchical metric learning).

3. **Cross-view tangent transport with a nuisance register:** predict a
low-rank augmentation tangent from one image and apply it to another same-class
image. NRQ, CoDiF, transformed-attention consistency, reconstruction, and
cross-image disentanglement occupy the components; `DEAD` by mechanism.

No GPU work is authorized. The only live-narrow item is (1), whose next gate is
a primary-art audit plus a CPU falsifier that distinguishes cross-instance
Jacobian transport from ordinary augmentation consistency.

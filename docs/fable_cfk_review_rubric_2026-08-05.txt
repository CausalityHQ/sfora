You are the fresh independent adversarial reviewer in a rigorous method-invention
protocol. Start cold. Do not inspect the local filesystem, repository, git
history, prior conversation, or critic notes. The frozen proposal follows this
rubric in the same prompt. Use web search and primary literature.

Legal task: zero-shot image similarity learning with disjoint train/test
identities on CUB-200-2011, Cars196, In-Shop, or SOP. Only official training
images, identity labels, ordinary augmentation, and ImageNet-1K initialization
are permitted. Deployment must be one model, one view, one fixed 512-D global
descriptor, ordinary cosine/Euclidean nearest-neighbour retrieval.

Matched Lane-A references are ResNet-50/512-D/single-view: PFML CVPR 2025 CUB
0.734 +/- 0.003, Cars 0.927 +/- 0.003, SOP 0.829 +/- 0.002 over five runs;
PA+DADA In-Shop 0.930 with no reported uncertainty. Do not mix capacity lanes.

Review the frozen CFK proposal without silently repairing, completing, or
replacing it. The durable provider record begins mid-section because its job
failed after streaming; treat every unavailable definition as unavailable.
Return LIVE, DEAD, or UNRESOLVED. Independently check:

1. every definition, symbol, normalization, constraint, limiting case,
   derivative, clipping/projection path, score expansion, regularizer, and cost;
2. whether every claimed gauge or anti-degeneracy invariant remains true after
   all parameter transformations, shrinkage, and bounds actually specified;
3. whether the claimed causal pressure to suppress within-class factors follows
   from the complete positive/negative objective, and whether discarded
   class-specific training parameters rule out memorization or merely move it;
4. exact mechanism-level prior art, especially Kirchhof et al. ECCV 2022
   non-isotropic probabilistic proxy DML, L-GM, Fisher-Bingham/Kent directional
   models, common-principal-component and semi-tied covariance models,
   heteroscedastic discriminant/metric learning, probabilistic embeddings, and
   any benchmark-matched DML method with class-conditional anisotropic scores;
5. whether the common learned frame and per-class diagonal precision are a
   substantive identifiable constraint or absorbable/gauge-equivalent under a
   learned embedding and normalized cosine deployment;
6. whether controls isolate anisotropy, class conditioning, common-frame
   structure, extra class parameters, generic optimization effects, and the
   claimed factor-preservation mechanism; verify each control's stated null;
7. whether the conditional composition with PFML is executable and matched,
   and whether any frozen forecast actually crosses the proper frontier with
   decision-relevant uncertainty; and
8. hidden data, contamination, inference, tuning, selection, recipe, capacity,
   and benchmark-protocol violations.

Any substantive repair is a different proposal and must not be credited. Cite
primary sources. Identify the single strongest decisive reason for the verdict
and flag every internally inconsistent, false, or underdefined frozen operation.

--- FROZEN PROPOSAL BEGINS ---
